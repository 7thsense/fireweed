# Local Object-Log Smoke Fixture

`local_object_log_deployment_smoke_tests` self-provisions runtime object-log and
SQLite projection state under the OS temp directory. This directory is only a
fixture anchor for the profile shape and documents that no checked-in object-log
state is required.

File writes and `cargo` were both denied in this session, so the round-40 review is below rather than at `docs/helix/04-build/reviews/writer-contention-round40/claude.md`.

**Session limitation:** `cargo` invocation was denied, so the newly recorded 0.7.0 dispositions were audited as source assertions (`local.rs:868-922`) and against the addendum's recorded values, not independently re-executed. Nothing below rests on my having re-run them.

## Findings

| Severity | Area | Finding | Required change |
| --- | --- | --- | --- |
| WARNING | Governing artifacts contradict the adapter pin S-0's evidence is keyed to (`TD-010:71`, `TD-010:197`, `ADR-016:58`, `third-party-notice-review.md:20` vs. `crates/fireweed-turso/Cargo.toml:25`, `Cargo.lock:4654-4670`, addendum `:115-116`) | S-0 declares its in-crate test "authoritative at the adapter's exact Turso pin", says the standalone probe "may never pin below the adapter", and "reruns on adapter bumps" (`:594`). The addendum records adapter 0.7.0 / probe 0.7.2 (`:115-116`), matching source: `crates/fireweed-turso/Cargo.toml:25` is `turso = { version = "=0.7.0" }` and the root `Cargo.lock` resolves `turso`/`turso_core` 0.7.0. But three governing documents in this same uncommitted change set assert 0.7.2 as the *adapter's* pin: TD-010's in-scope list ("`fireweed-turso`, pinned to Turso 0.7.2"), TD-010 External Dependencies (`turso = "=0.7.2"`), ADR-016 ("The supported boundary is embedded/local `turso = 0.7.2`"), and the legal notice ("`turso_core 0.7.2`, reached through the workspace's `fireweed-turso` package"). `crates/fireweed-bench/Cargo.lock` likewise now resolves `turso`/`turso_core` 0.7.2 against a stale `fireweed-turso 0.31.16` entry. None of this is in the claimed bead's scope (`fireweed-5dbe1026` owns only the probe, `local.rs`, and the addendum), and no bead mentions 0.7.2. The consequence is not cosmetic: every disposition this fold recorded — `Ok([])`/no readback for `wal_autocheckpoint`, `Ok([])`/readback `1` for `cache_spill`, the `turso::Error::Error` text — is an exact-version fact. If 0.7.2 is the intended adapter pin, the plan's own rule invalidates all of it before S-0 can close; if 0.7.0 is normative, three governing artifacts misstate the pin the entire S-0 authority chain rests on. | State which pin is normative in one place. If 0.7.0: correct `TD-010:71`, `TD-010:197`, `ADR-016:58`, and the legal notice, and reconcile `crates/fireweed-bench/Cargo.lock`. If 0.7.2: bump `crates/fireweed-turso/Cargo.toml`, re-run and re-record every S-0 disposition at that pin before S-0 closes, and restate the "never pin below the adapter" rule against the new value. Do not close S-0 while both answers coexist. |
| NOTE | S-0's serving gate omits `server_pending_range` (`:594` vs. `:595`, `:604`, `projection.rs:1767`) | The `serving_reader_is_query_only_committed_and_nonblocking` gate enumerates "validation, `render_claimed`, `server_pending_page`, `server_live_items`, `server_metrics`, and recovery reads". S0's new observation cohort and S3r's before/after clause both name four `server_*` operations including `server_pending_range`; the S-0 gate names three. The underlying read is in fact covered — `server_pending_page` (`projection.rs:1743`) and `server_pending_range` (`:1767`) both delegate to `server_pending_by_ids` (`:1692`) — so this is an enumeration asymmetry, not an ungated query path. It still reads as a fourth consumer deliberately excluded from the corrected-serving gate. | Add `server_pending_range` so the three clauses enumerate the same four operations, or state that `server_pending_by_ids` is the gated read and both entry points inherit it. |
| NOTE | The addendum's replacement-gate paragraph still carries the wordings the plan body corrected (`turso-0.7-compatibility-probe-results.md:183-185`, `:191-192`, `:157-163`) | The fold updated the addendum's *facts* (items 3–5 at `:137-145` record all three dispositions, closing round 39's finding 1) but not its *narrative*. `:183-185` still says the probe "records whether uncommitted frames were published; either disposition is valid" — the exact publication overclaim round 39's finding 5 rejected and the plan replaced with `no_uncommitted_wal_growth_observed` / `inconclusive-no-growth-observed`. `:191-192` still says the separate connection "agrees after commit", the ambiguous wording finding 6 flagged and the plan fixed to "must observe the third writer's value". `adversarial_spill_unavailable` and the new labels appear nowhere. The command block at `:157-163` names only the two superseded tests; nothing names `records_v03121_read_uncommitted_keyword_disposition`, which produced items 3–5. | Sync the paragraph to the plan's current S-0 text: replace the publication phrasing with the two labels, state the third-writer expected value, name `adversarial_spill_unavailable`, and add the command that produced items 3–5 so the dispositions are reproducible from the evidence document alone. |
| NOTE | The `-wal` length instrument's monotonicity rests on the one pragma proven unattestable (`:594`, `local.rs:1306`) | S-0 samples `-wal` length at three points "without checkpoint". That is enforceable for *explicit* checkpoints (S3r's source audit/counter), but the fold's own evidence is that `wal_autocheckpoint=0` returns `Ok([])` with no readback, so an automatic checkpoint during the 800-row/4 MiB adversarial write cannot be excluded by attestation. If one fires, the WAL can reset between samples and the delta can be zero or negative for reasons unrelated to frame publication. The escape hatch triggers only when "adversarial settings cannot be attested" — which reads as `cache_size`/`cache_spill`, both of which *do* read back — so the un-attestable knob is not what selects `inconclusive-no-growth-observed`, and no branch describes a negative delta. Costs nothing at the gate (growth is explicitly diagnostic, both dispositions pass), but it mislabels the diagnostic. | Name `wal_autocheckpoint` in the inconclusive trigger, and record a non-monotonic delta as `inconclusive-no-growth-observed` with observed checkpoint/WAL behavior — the wording S3r already uses for `twenty_four_pooled_committed_readers_do_not_restore_47b1a223_wal_freeze`. |
| NOTE | A failing S3r cache trial has no stated consequence for the 4 MiB pools S3c activates (`:604`, `:610`, `:483-491`, risk table `:992-1000`) | Round 39's finding 2 is correctly discharged: regression restores 128 MiB, records 352 MiB, and does not block pool construction. But the trial measures exactly the change S3c makes permanent — the same serving reads move onto twenty-four pooled connections at 4 MiB each, unconditionally, in the next activation slice. The plan's only early measurement of 4 MiB serving reads is therefore recorded and carries no obligation. S3c's "Failure/non-regression breach reverts to prior serving" catches it late, the 224 MiB arithmetic has no branch if pools need more than 4 MiB each, and the risk table has no row for it. | State what a failing trial obliges before S3c: at minimum record it as a predicted-regression risk with S3s re-derivation of pool cache sizing inside the 224 MiB envelope, and add the Risk/Detection/Rollback row. No architecture change implied — only naming the consequence of the branch the fold made legal. |
| NOTE | The helper has no role for the adversarial writer (`:594`) | The helper is now parameterized by cache size and busy timeout and specified as "then numeric `query_only=1` last", correctly closing round 39's finding 4 for readers. Its two roles — serving (128 MiB, product timeout) and pool candidate (4 MiB, 100 ms) — are both query-only. The adversarial writer needs a third combination (4 MiB, `cache_spill=1`, *not* query-only), and the plan says only that it "uses 4 MiB plus attested `cache_spill=1`", never which readbacks apply. `journal_mode`/`synchronous`/`cache_size`/busy timeout plainly should; `query_only=1` plainly must not. | Name a writer role (or state the writer is configured outside the helper) and list exactly which readbacks it must assert, so "attested" has a fixed meaning for the `inconclusive-no-growth-observed` branch. |

## Prior-round audit

Round 39's **WARNING** on the helper pragma contract is **closed**. The fold did what the finding asked, in the right order: run the dispositions first, then assign stop rules. `local.rs:868-922` drives `wal_autocheckpoint=0`, `cache_spill=1`, and a query-only write on a 0.7.0 connection and asserts each result (`Ok([])`/`None`, `Ok([])`/`Some(1)`, `turso::Error::Error` containing `Cannot execute write statement in query_only mode`); the addendum records all three as numbered facts (`:137-145`). `wal_autocheckpoint` is correctly classified accept-without-readback and kept non-fatal, matching production's deliberate `let _ =` at `local.rs:1306`, and S3r's checkpoint claim was rewritten from `checkpoint_disposition=disabled_no_checkpoint_invoked` to a source audit plus observed WAL behavior, removing the dependency the finding identified. `cache_spill` has both a disposition and a named non-stopping failure branch. All five hard-stop knobs have recorded 0.7.0 evidence: `journal_mode`; `synchronous` (production sets `OFF` at `local.rs:1300` and verifies `!= 0` at `:1338`, so the plan's `synchronous=0` matches source rather than the 2026-07-17 table's rusqlite-era `synchronous=1`); `cache_size` and `busy_timeout` (both in the standalone output at addendum `:170`); numeric `query_only=1`. Making the live serving reader fatal on `query_only=1` is now evidence-backed. The residual is only *which release* those facts belong to — finding 1.

Round 39's **WARNING** on S3r's mandatory cache reduction is **closed**. `:604` and `:483-491` are reconciled: the trial keeps its before/after measurement, passing records 228 MiB, failing restores 128 MiB and records 352 MiB through S3c, both branches construct pools, both converge to 224 MiB after retirement. The arithmetic checks (128 + 4 + 96 = 228; 128 + 128 + 96 = 352; 128 + 96 = 224; recovery 128 + 4 = 132). The strongest legitimate outcome is no longer routed to a program stop. What remains is the unnamed downstream implication, which is a NOTE.

The seven round-39 NOTEs. **`server_*` instrument folded** — S0 carries a bounded observation cohort with per-operation rate/p50/p95/p99 and exact response counts, named in S3r's gate; residual is the one-name enumeration gap. **Helper parameterization folded** — cache size and busy timeout are independent with two named roles; residual is the missing writer role. **`uncommitted_frames_published` label folded** in the plan; residual is the addendum sentence and the inconclusive trigger. **Third-writer corroboration folded** — the expected value is now explicit and inverts nothing; residual is addendum wording only. **Stale-page observable folded** — every row of the >4 MiB set carries the current committed round number, which is separable from the newest-version assertion and detects a retained round-N−1 page. **Typed query-only rejection class folded** — variant and message class named in the plan, asserted at `local.rs:917-921`, recorded in the addendum. **Frontmatter folded** — `round: 39`, `claude: REVISE`, `folded: …round39/`, and the disposition line are mutually consistent and agree with `:1053-1064`.

Rounds 36–38 dispositions are undisturbed. The cited N=10k evidence reconciles exactly with `docs/perf/evidence/ss-phased/1787274546/summary.json` (31,780.1 / 34,898.0 / 49,911.6 / 1,289.8 items/s; claim p50 211.184 ms; finalize p50 333.483 ms; schema v3, superseded by S0's v4). No admission arithmetic, fence disposition table, publication/response ceiling (505/540, 2,021.075/2,056.075, 17,246.775/17,281.775, 4,040/4,075, 64,640/64,675 s), retained-response accounting (8 + 24 drivers × 4 MiB = 128 MiB), or reclaim bound changed in this fold, and none depends on the S-0 attestation mechanism. Protocol step 5's `read_uncommitted` removal (`:691-703`) and step 7's snapshot-pinning neutrality are consistent with S-0 semantics being proven rather than configured.

## Verdict

`REVISE`

## Convergence

`NO`

## Summary

Both round-39 warnings are genuinely discharged, and discharged the right way round: the pragma dispositions were measured before stop rules were assigned, `wal_autocheckpoint` was reclassified rather than forced, `cache_spill` gained a non-stopping branch, and the S3r cache trial now has two legal outcomes with consistent 228/352/224 MiB arithmetic and no downstream halt. All seven notes are folded in the plan body. One thing blocks convergence, and it is not in the plan's reasoning — it is in the artifacts the plan governs. TD-010, ADR-016, and the legal notice review now assert `turso = 0.7.2` as the adapter pin while `crates/fireweed-turso/Cargo.toml` and the root lock hold `=0.7.0`, which is the release every disposition this fold recorded was measured against and the release the addendum names. S-0's authority is stated as "authoritative at the adapter's exact Turso pin" with a mandatory rerun on bumps, so the two answers cannot both stand while S-0 closes: either the documents are wrong, or the evidence is already stale. Resolve that one fact and the remaining five notes — the `server_pending_range` enumeration, the addendum's un-synced narrative, the checkpoint caveat on the WAL instrument, the unnamed consequence of a failing cache trial, and the missing writer role — all fit inside existing named acceptance criteria.

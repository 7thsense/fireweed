File writes were denied in this session, so the round-41 review is below rather than at `docs/helix/04-build/reviews/writer-contention-round41/claude.md`.

**Session limitation:** `cargo` was not invoked. The 0.7.0 dispositions were audited as source assertions (`crates/fireweed-turso/src/local.rs:867-922`) and against the addendum's recorded values, not re-executed. Manifest/lock pins, the `projection.rs` read surface, and the single shared reader connection were read directly from the v0.31.21 working tree.

## Findings

| Severity | Area | Finding | Required change |
| --- | --- | --- | --- |
| NOTE | S-0's serving gate still under-enumerates the shared reader's consumers (`:604`, `:605` vs. `projection.rs:1604`, `:1631`, `local.rs:233`, `:361`) | Round 40's `server_pending_range` gap is closed, but that name was safe precisely because it delegates to `server_pending_by_ids` (`:1692`). `server_peek` (`:1604`) and `server_pending` (`:1631`) do not delegate: each issues its own `self.query(...)` SELECT, and `TursoRelational::query` (`local.rs:360-363`) locks the one `Arc<Mutex<Connection>>` serving reader (`local.rs:233`) that S-0 is converting to numeric `query_only=1`. Neither appears in `serving_reader_is_query_only_committed_and_nonblocking`. The safety risk is nil — both are pure SELECTs, and I found no non-SELECT statement routed through `query()`, so `query_only=1` cannot break them — but the measurement consequence is real: S0's observation cohort (`:605`) is the *only* before/after basis S3r has for the 128 MiB→4 MiB trial (`:614`), and it covers four of six live serving reads. A trial that passes on `server_pending_page`/`server_pending_range`/`server_live_items`/`server_metrics` says nothing about `server_peek`/`server_pending` at 4 MiB, and those two move onto the same pooled connections at S3c. (`server_pending_summary` at `:1664` reads only in-memory `live_tokens` and correctly needs no gate; `server_terminal_emission_metrics` at `:1916` delegates to `server_metrics`; `server_update_snapshot` at `:1804` is plausibly the gate's named "validation" but is not said to be.) | Add `server_peek` and `server_pending` to the S-0 gate and to S0's observation cohort, and state that `server_update_snapshot` is the named validation read. Both fit inside existing acceptance criteria. |
| NOTE | The folded WAL-instrument trigger is unconditionally true at the normative pin, so the label has one reachable value (`:604`, addendum `:188-190` vs. `local.rs:908-911`, addendum `:137-140`) | The fold reads: "Record growth with byte delta or `no_uncommitted_wal_growth_observed`; any non-monotonic delta, unavailable autocheckpoint attestation, or unattested adversarial setting is `inconclusive-no-growth-observed`." But S-0's own recorded 0.7.0 fact — asserted three lines earlier in the same gate, at `local.rs:908-911`, and as addendum item 3 — is that `wal_autocheckpoint` has *no readback at all*. Under the plain reading, "unavailable autocheckpoint attestation" is satisfied on every run at the normative pin, `inconclusive-no-growth-observed` becomes the only reachable disposition, and both the byte-delta and `no_uncommitted_wal_growth_observed` branches are dead — including when a clean monotone positive delta is observed. S3r's parallel wording (`:614`) shows the intended distinction: it separates the "unavailable `wal_autocheckpoint` readback" from "observed automatic-checkpoint/WAL behavior", which *is* obtainable. The addendum compounds this by pairing two triggers and two labels with a bare slash (`:188-190`), leaving `adversarial_spill_unavailable`'s trigger unbound. Costs nothing at the gate — growth is explicitly diagnostic and every label passes — but it turns the instrument round 38 asked for into a constant. | Bind the trigger to observation, not to the pragma: name a non-monotonic delta or an observed/undeterminable checkpoint during the window as `inconclusive-no-growth-observed`, and bind `adversarial_spill_unavailable` to `cache_spill` alone. Sync the addendum's slash pairing to the same mapping. |
| NOTE | A concurrent uncommitted edit to governing artifact #4 asserts an explicitly out-of-scope architecture as implemented (`ss-objectlog-turso-memory-goal.md` working-tree diff vs. `:45-48`, `:58`, `:74-76`) | The fold's out-of-scope enumeration at `:74-76` names the TD-010/ADR-016/legal/benchmark-lock 0.7.2 edits, and it is accurate for the pin. But the same uncommitted change set also rewrites `ss-objectlog-turso-memory-goal.md`, which the plan lists as governing authority #4 (`:45-48`) and as an S1 bounded file. The added text specifies a "Log-ordered planner map … Advances at plan/append, not at Turso apply", "Zero Turso plan queries, no `validate_push` writer txn, no `snapshot_live_items` catch-up on produce", and a map holding metadata "for the process lifetime of the queue" — then states "The revised cut is below and is what the `filesystem--turso` product now implements." The plan's out-of-scope list at `:58` names exactly "a process-lifetime item planner map", and source contradicts the implementation claim: no `planner_map`/`PlannerMap` symbol exists anywhere in `crates/`, while `validate_push` (`projection.rs:1952`) and `snapshot_live_items` (`turso_compose.rs:229`, `:1698`) are both live. The doc also labels evidence `1786977588`/`1786977711` (P4 315/s at N=10k) as "Latest" while the plan cites the newer committed `1787274546` (P4 1,289.8/s). No plan claim depends on the doc's architecture section and its M1/M2/M3 definitions are untouched, so no evidence is invalidated — this is an authority-chain inconsistency, not stale evidence. | Do not rewrite the user's file. Extend the `:74-76` classification to name the `ss-objectlog-turso-memory-goal.md` edit as concurrent out-of-scope work that does not amend `:58`, and add reconciling its planner-map/"latest evidence" claims to S1's existing `governing_microbatch_contract_present` scope — that file is already an S1 bounded file. |
| NOTE | The 224 MiB envelope S3s must re-derive inside is exactly saturated, so the mitigation has no free variable (`:614`, `:1009`, `:491-501`) | Round 40's finding 5 is discharged in form: S3r flags a failing trial as a predicted pool-cache regression, S3s owns profiling/re-derivation, S3c is blocked if that fails, and the risk table (`:1009`) carries detection and rollback. But the envelope is fully consumed by the plan's own normative constants: 128 MiB writer + sixteen driver connections × 4 MiB + eight outcome connections × 4 MiB = 224 MiB exactly (`:491-501`). Connection counts are normative and 224 MiB is a recorded ceiling in memory gate 5 (`:990-992`). So "re-derive pool cache sizing inside the 224 MiB post-S3c envelope" admits no larger per-connection cache without moving a constant S3s is not authorized to move, and the mitigation collapses to its own block branch. The plan is not wrong — it already names "if it cannot, block S3c activation and keep prior serving" — but the intermediate path is arithmetically empty as written. S3s's own gate text (`:619`) also never mentions the duty; only S3r's gate and the risk row do. | State which variable S3s may move (pool connection counts, the 128 MiB writer cache, or the 224 MiB ceiling re-recorded through memory gate 5), and name the carried duty in S3s's exit gate. Both fit existing criteria. |

## Prior-round audit

Round 40's **WARNING** on pin authority is **closed**. The fold picked one answer and stated it in one place (`:71-79`): the normative adapter dependency for S-0 is the executable pair on the claimed v0.31.21 base, and it is 0.7.0. I verified both halves — `crates/fireweed-turso/Cargo.toml:25` is `turso = { version = "=0.7.0" }` and root `Cargo.lock:4654-4670` resolves `turso` and `turso_core` at 0.7.0 — so the pin every disposition was measured against is the pin the plan declares normative, and the addendum's `:113-116` pair statement agrees. The rule that "a nested benchmark lock or document-only edit cannot change that pin" is defensible rather than convenient: `crates/fireweed-bench` and `tools/fireweed-turso-compat-probe` are independently routed workspaces excluded from `cargo test --workspace` (root `Cargo.toml:7-9`), and every evidence lane the plan names runs from root-workspace tests (`crates/fireweed/tests/ss_phased_capacity.rs` exists; `ss_mixed_overlap.rs` is S0's new file). So the 0.7.2 benchmark lock cannot reach a T/M artifact. The standalone probe at 0.7.2 satisfies "may never pin below the adapter". The escape clause — one atomic manifest/root-lock/governed-artifact bump plus a complete S-0 rerun that supersedes the 0.7.0 evidence — is the right shape and is anchored in S-0's own "reruns on adapter bumps". The user's 0.7.2 edits are untouched.

Round 40's five NOTEs. **`server_pending_range` folded** — it now appears in the S-0 gate (`:604`), S0's cohort (`:605`), and S3r's before/after clause (`:614`), so all three enumerate the same four operations; residual is the two non-delegating reads above. **Addendum narrative folded** — the publication overclaim is gone and replaced by the two labels (`:188-190`), the third-writer sentence states the expected value ("must see the newest committed value, not the live dirty value", `:195-198`) and matches the plan's "never the third writer's value", `adversarial_spill_unavailable` is named, and the command block (`:157-165`) now includes `records_v03121_read_uncommitted_keyword_disposition`, so items 3–5 are reproducible from the evidence document alone. **WAL caveat folded** — `wal_autocheckpoint` is named in the inconclusive trigger and a non-monotonic delta has a branch; residual is that the trigger never turns off. **Cache-trial consequence folded** — S3r flags the predicted regression, S3s owns re-derivation, S3c blocks if it fails, and the risk row carries detection and rollback; residual is the saturated envelope. **Writer role folded** — the adversarial writer is explicitly configured outside the query-only reader helper with file-backed WAL, `synchronous=0`, cache `-4096`, the product busy timeout, attested `cache_spill=1`, and no `query_only` (`:604`), and "unattested adversarial setting" now has a named non-stopping disposition.

Rounds 36–39 dispositions are undisturbed. The 0.7.0 disposition test (`local.rs:867-922`) asserts exactly what the addendum records: `read_uncommitted="ON"` → `Ok([])`/`None`; `wal_autocheckpoint=0` → `Ok([])`/`None`; `cache_spill=1` → `Ok([])`/`Some(1)`; numeric `query_only=1` then a write → `turso::Error::Error` containing `Cannot execute write statement in query_only mode`. It runs file-backed against a tempdir path, not in memory. The discarded `pragma_update("query_only", "ON")` / `pragma_update("read_uncommitted", "ON")` pair the plan calls a release-baseline defect is at `local.rs:272-273`, and the serving reader really is one mutex-guarded connection (`local.rs:233`), which is what makes the S-0 conversion coherent and the enumeration gap measurable rather than dangerous. The N=10k evidence reconciles with `docs/perf/evidence/ss-phased/1787274546/summary.json` (schema v3; 31,780.1 / 34,898.0 / 49,911.6 / 1,289.8 items/s), which is tracked. Every injected ceiling recomputes: fence holder 5+5+30+30 = 70 → 75 s acquisition; slot 5+75+5+5 = 90 → 95 s; turn/sequencer 5+95+5+75+5+5+60 = 250 → 255 s; service 255+5+95+5+75+5+5+60 = 505 → 540 s response; 4×505+1+0.075 = 2,021.075 → 2,056.075; 31×540+505+1+0.775 = 17,246.775 → 17,281.775; 8×505 = 4,040 → 4,075; 128×505 = 64,640 → 64,675; 17×540+2 = 9,182; 16×540 = 8,640; 3×10+1+0.05 = 31.05. Memory arithmetic recomputes: 228, 352, 224, 132, and 32×4 = 128. Frontmatter is self-consistent (`round: 40`, `claude: REVISE`, `folded: …round40/`, disposition) and agrees with `:1064-1075`.

## Verdict

`APPROVE`

## Convergence

`YES`

## Summary

Round 40's warning is closed on the evidence rather than by assertion: the adapter manifest and root lock do hold `=0.7.0`, the benchmark lock carrying 0.7.2 sits in an independently routed workspace no named evidence lane builds through, and the plan now says so in one place with a concrete supersession rule for a future bump. The user's concurrent 0.7.2 edits are preserved untouched. All five notes are folded, three of them fully — `server_pending_range`, the addendum narrative, and the adversarial writer role. What remains is four items that sit inside acceptance criteria the plan already names, change no architecture and no public contract, and invalidate no recorded evidence: two shared-reader consumers (`server_peek`, `server_pending`) missing from the S-0 gate and, more consequentially, from the S0 cohort that is S3r's only cache-trial basis; a WAL-instrument trigger that is unconditionally true at 0.7.0 and so leaves the diagnostic with one reachable label; a concurrent edit to governing artifact #4 asserting a process-lifetime planner map as implemented when `:58` forbids it and no such symbol exists in the tree; and a 224 MiB envelope that is exactly saturated, so the S3s re-derivation the fold introduced has no free variable unless the plan names one. None is a reason to hold S-0. Converged.

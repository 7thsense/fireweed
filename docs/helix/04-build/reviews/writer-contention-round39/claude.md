The review file write wasn't permitted in this session, so here is the round-39 review in full (intended path: `docs/helix/04-build/reviews/writer-contention-round39/claude.md`).

## Findings

| Severity | Area | Finding | Required change |
| --- | --- | --- | --- |
| WARNING | S-0 helper pragma contract vs. product open (`writer-contention-recovery-plan.md:593`, `local.rs:1249-1266`) | The fold closed round 38's "unstated pragma set" NOTE by making the set complete *and* fatal: "Both require `journal_mode=wal`, `synchronous=0`, numeric `cache_size`, `wal_autocheckpoint=0`, exact busy timeout, then numeric `query_only=1` last; setter failure or supported-readback mismatch hard-stops," and the live serving reader is corrected "through the same helper … propagate configuration failures." Two of the required knobs have no recorded Turso 0.7 disposition anywhere in the repo. `wal_autocheckpoint` is the one pragma production deliberately tolerates — `let _ = connection.pragma_update("wal_autocheckpoint", "0").await` (`local.rs:1265`, added by `aa326d7d4`) — it is absent from `read_connection_settings` (`local.rs:1315-1320`), absent from the probe, and absent from the addendum's readback table (`turso-0.7-compatibility-probe-results.md:128-136`), which records exactly this failure mode for `read_uncommitted` (accepts, returns no readback row). `cache_spill` appears nowhere in the workspace or the probe at all. So the plan (a) may hard-stop S-0 on an instrument rather than a property, and (b) converts `TursoRelational::open` from tolerant to fatal on that pragma for every deployment, on no evidence. If `wal_autocheckpoint` turns out to be accept-but-no-readback, it sits in precisely the class this redesign purged for `read_uncommitted` while S3r's `checkpoint_disposition=disabled_no_checkpoint_invoked` evidence depends on it being effective. | Record each pragma's exact 0.7.0 disposition (setter result, readback presence) as S-0 evidence *before* assigning it a stop rule, as was done for `read_uncommitted="ON"`. Classify `wal_autocheckpoint` as supported-with-readback (then hard-stop and add it to `read_connection_settings`), or accept-without-readback (then record it, do not hard-stop, and state what S3r's checkpoint disposition rests on instead). Name the writer's `cache_spill` failure disposition explicitly; it is currently neither a "semantic" nor a "supported-readback" failure, so S-0's stop rule does not cover it. Do not make the live serving reader's open fatal on a pragma the product currently tolerates without that evidence. |
| WARNING | S3r mandatory serving-cache reduction has no non-stop branch (`:603`, `:483-490`) | S3r now reads: "First lower the still-live shared reader from 128 MiB to 4 MiB through S-0's helper and require a same-SHA before/after S0-harness rate delta to meet the ordinary >=90% and p95/p99 <=125% gates … failure leaves pools unconstructed." Every later slice (S3g, S3q, S3s, S3c, S3m, S5, S8c, S8q) depends on S3r, so a rate regression halts the program. What it halts on is self-inflicted: the shared reader is *retired* at S3c (`:488`, `:609`), so the reduction configures a component scheduled for deletion, and the only thing it buys in the interim window is the 228-vs-352 MiB number — a figure this same fold demoted to "configured ceilings, while M1/M2/M3 remain the behavioral memory gates" (`:489-490`). A 32× page-cache cut on the single `Arc<Mutex<Connection>>` reader (`local.rs:232-233`, `:361`) that carries `render_claimed` and all validation reads is a plausible regression, and "the serving reader needs its 128 MiB" is a correct outcome, not a program stop. This is the shape round 38 blocked on in S-0 (strongest legitimate outcome routed to a stop), relocated to S3r; round 37's and round 38's own stated alternative — keep the product cache and state the interim ceiling explicitly — was dropped rather than retained as the failure branch. | Keep the before/after measurement, but make its failure branch "retain 128 MiB, record the interim configured ceiling (352 MiB) through S3c, and construct the pools" rather than "pools unconstructed." Reconcile `:483-490` so the interim figure is a recorded observation with two legal values, not a prerequisite. If the reduction is genuinely required before S3c, state the requirement it serves; the memory gates as written do not supply one. |
| NOTE | S3r's `server_*` clause has no instrument (`:603`, `:594`) | The gate requires the delta "including `render_claimed` and `server_*`", but the S0 harness contains no observation lane: `crates/fireweed/tests/ss_phased_capacity.rs` (965 lines) never references `server_pending_page`, `server_pending_range`, `server_live_items`, or `server_metrics`, and `ss_mixed_overlap_baseline` as specified at `:594` records Push/Claim/Complete rates, fill, byte distribution, and wait/starvation — no observation reads. `render_claimed` is covered by P4; the four `server_*` consumers S-0 newly enumerated are not. The 32× cut therefore still lands unmeasured on those paths, which is the residue of round 38's WARNING. | Add a bounded `server_*` observation lane (pending page/range, live items, metrics) to `ss_mixed_overlap_baseline` at S0 and name it in S3r's before/after gate, or drop `server_*` from the clause and state that those paths are covered only by the S3c retirement. |
| NOTE | Helper roles bind cache size to timeout (`:593`, `:603`) | The helper is parameterized by reader role: "serving uses the product busy timeout and preserves its 128 MiB cache through S0; pool candidates use 100 ms and 4 MiB." S3r then needs serving-timeout **with** 4 MiB — a combination neither role expresses. Round 38's NOTE asked for a parameterized timeout; the fold parameterized the pair. | Parameterize cache size independently of busy timeout (or name a third `serving-reduced` role), and state what verify asserts for each combination. |
| NOTE | `uncommitted_frames_published=false` is labeled "the stronger valid disposition" (`:593`) | The label conflates a property of Turso ("never publishes uncommitted frames") with an experiment outcome ("this configuration failed to induce publication"). Nothing attests that the adversarial knobs took effect: `cache_spill` has no readback in the plan, and this same fold declares `cache_size` a *configured* ceiling attested only by readback (`:489`). `false` is therefore indistinguishable from inert knobs. Nothing downstream reads the flag — step 5 rests on the semantic assertions (`:690-694`) — so this is a labeling defect in the recorded evidence, not a gate defect. | Record `false` as `inconclusive-no-growth-observed` unless the writer's adversarial configuration is positively attested (readback of `cache_size`/`cache_spill`, or a positive control such as page-count/WAL growth inside the uncommitted window), and say which. |
| NOTE | Third-writer corroboration is under-specified (`:593`) | "A third writer then holds an uncommitted newest value while every reader … sees the second writer's newest committed value, never the third writer's value. A separate configured non-writer connection agrees after the third commit." Read literally, "agrees" points at the readers' pre-third-commit observation, which after the third commit would assert staleness — the inverse of the freshness property S3r/S5 rest on. The pooled readers' second snapshots do prove freshness on their own (they observe writer 2's commit, made after their connections existed), so this is corroboration wording, not the load-bearing witness. | State the expected value explicitly: after the third commit, the separate non-writer connection must observe the third writer's value. |
| NOTE | "No stale-page reuse" has no named observable (`:603`) | `committed_pools_construct_isolated_connections` must run "eight successive commit→snapshot rounds per pooled connection over a result set larger than 4 MiB … asserting the newest committed version, no dirty value, and no stale-page reuse." The first two are value assertions; the third names no distinct observable and is not separable from them. | Either define the observable (e.g. every row of the >4 MiB result set carries the current round's version, so a retained page from round N−1 is detectable) or fold the phrase into the newest-version assertion. |
| NOTE | Typed query-only rejection class unnamed (`:593`) | "Twenty-four candidate connections prove typed query-only rejection both inside a snapshot and in autocommit after it closes" closes round 38's attribution NOTE structurally, but does not name the `turso::Error` variant the assertion matches — the same gap that produced the `query_only="ON"` keyword defect. `Busy`/`BusySnapshot` are named concretely and do exist (`tools/fireweed-turso-compat-probe/src/main.rs:186`). | Name the expected error variant/message class, and record it in the addendum alongside the `read_uncommitted` disposition. |
| NOTE | Frontmatter disposition contradicts the body (`:14-19`) | Round/verdict/folded path are now current (`round: 38`, `claude: REVISE`, `folded: …round38/`), closing the mechanical half of round 38's NOTE. The `disposition` line still reads "round-38 forced-spill observability blocker and serving-reader cache warning pending fold" while `:1052-1058` states both are folded and awaiting confirmation. | Set `disposition` to the post-fold state ("round-38 blocker and warning folded; awaiting round-39 confirmation"). |

## Prior-round audit

Round 38's **BLOCKING** S-0 forced-spill/WAL-growth finding is **closed** on all four sub-defects. (a) Satisfiability is resolved by abandoning the representativeness framing outright: the writer is now "deliberately adversarial" at 4 MiB rather than production-configured at 128 MiB (`local.rs:1261`), which is the honest resolution — under production configuration a ~4 MiB dirty set cannot spill, so the adversarial writer is strictly stronger where it works. (b) The sample point is now three named samples with the decisive one "strictly after the updates but before commit". (c) The instrument is `-wal` file length with no checkpoint invoked, on a file-backed database, which no longer contradicts S3r's `disabled_no_checkpoint_invoked`. (d) The no-growth branch is a pass, not a stop. Residuals are attestation and labeling only (two NOTEs).

Round 38's **WARNING** on serving-reader correction scope is **closed as to sequencing and scope, and re-raised at the same severity for its new disposition**. Scope is now complete and matches source: the four `self.reader.clone()` sites (`projection.rs:1958`, `:2084`, `:2109`, `:2247`) plus every `TursoRelational::query` consumer (`local.rs:361`) — `render_claimed` (`:2728`), `server_pending_page` (`:1743`), `server_pending_range` (`:1767`), `server_live_items` (`:1858`), `server_metrics` (`:1895`), `recover_definitions` (`:2907`) — are enumerated in S-0's named serving gate, and all of them are read-only, so `query_only=1` on that connection is safe. Preserving 128 MiB through S0 removes the "unmeasurable before the only rate baseline" defect entirely. What replaced it is a hard prerequisite with one failure branch (finding 2) and a measurement clause the S0 harness cannot discharge (finding 3).

The remaining round-38 NOTEs: freshness sequencing under a third live writer **folded** (wording residual); freshness depth **folded** — eight rounds, >4 MiB, live next writer per pooled connection; non-blocking sharpness **folded** — `Busy`/`BusySnapshot` rejection plus a no-writer control maximum, both grounded in real error variants; query-only attribution **folded** (variant unnamed); helper specification **partly folded** — roles and a complete pragma set exist, but the role parameterization is incomplete (finding 4) and the stop disposition is the subject of finding 1; standalone-probe pinning **closed** (in-crate authoritative at `=0.7.0`, probe corroborative at `=0.7.2`, never below the adapter, rerun on bumps); `cache_size` enforcement **closed** by relabeling to configured ceilings with S3r RSS and M1/M2/M3 authoritative; keyword disposition **closed** — `records_v03121_read_uncommitted_keyword_disposition` now asserts `Ok([])` and `None` (`local.rs:879-880`) instead of only printing; frontmatter **partly folded** (finding 8).

Round 36's architectural conclusions remain untouched. The fold is 73 inserted / 15 deleted lines confined to frontmatter, a new Objective section, the release-reconciliation recheck, the page-cache arithmetic at `:483-490`, S-0, S3r, protocol steps 5 and 7, and a new Open-questions section. No admission arithmetic, fence disposition table, publication/response ceiling (505/540, 2,021.075/2,056.075, 17,246.775/17,281.775, 4,040/4,075, 64,640/64,675 s), retained-response accounting, or reclaim bound changed, and none of them depends on the S-0 attestation mechanism. Round 36's eight NOTEs and dispositions stand.

## Verdict

`REVISE`

## Convergence

`NO`

## Summary

The round-38 blocker is genuinely discharged. Naming `-wal` file length as a non-mutating instrument, fixing the decisive sample strictly before commit, admitting an explicitly adversarial rather than representative writer, and making both growth branches pass on the semantic assertion together remove every defect that made the previous clause unsatisfiable. Preserving the serving reader's 128 MiB through S0 removes the measurement gap that made the serving-cache cut invisible, and the S-0 enumeration now matches the actual consumer set in `projection.rs`/`local.rs` — all of which are read-only, so query-only on the shared reader is safe. Two things stop convergence, and both are dispositions rather than mechanisms. First, the helper's hard-stop pragma set now includes `wal_autocheckpoint`, the one pragma production deliberately tolerates with a discarded result and the one with no recorded 0.7.0 disposition, and applies that stop to the live product reader with propagated failures; `cache_spill` is required on the writer with no disposition at all and appears nowhere in the workspace. Record those dispositions the way `read_uncommitted="ON"` was recorded before assigning either a stop rule. Second, S3r's 128→4 MiB reduction is a hard prerequisite whose only failure branch halts every downstream slice, over a configured ceiling this same fold declared non-authoritative, for a reader S3c deletes — restore round 37's own fallback as the failure branch. The remaining seven notes, including the missing `server_*` observation lane behind S3r's own gate clause, fit inside the existing named acceptance criteria.

---

Two notes on the session itself: writing to `docs/helix/04-build/reviews/writer-contention-round39/` was denied, so the review above is un-persisted. I also could not reach the cargo registry or the network, so Turso 0.7's actual support for `wal_autocheckpoint` and `cache_spill` is unverified — finding 1 rests on the absence of any in-repo record either way, which is itself the defect.

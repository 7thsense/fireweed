# TP-002 E3 — object-log cost model ($/billion-commands)

**Spec:** `tp-scale-substantiation` (bead `pqueue-1cd90d1c`). **Substantiates:** ADR-001 "Napkin Cost Comparison" — turns its *directional* claim that batched object-storage commits beat an always-on relational authority at high volume into a reproducible, fixture-tested calculation over the REAL E3 measured counts.

This document is GENERATED by `pqueue-cost-model` (a pure calculator in `pqueue_release::cost`). Do not hand-edit — regenerate with the command below.

## Reproducible command

```
cargo run -p pqueue-release --bin pqueue-cost-model -- --out docs/perf/tp002-e3-cost-model.md --ledger docs/perf/evidence/tp002-e3-cost-model.jsonl
```

## Headline

At the documented TP-002 high-volume baseline, using the REAL E3 measured counts (`E3 measured (size-dominant: 4 KiB target / 1000 ms cap)`) and the cited prices below:

| Backend | $/billion-commands |
|---|---|
| `object_log_sqlite_projection` | **$292.80** |
| `postgres_native` | **$984.04** |

`object_log_sqlite_projection` is **3.36x cheaper** than `postgres_native` at this baseline — the ADR-001 direction holds with honest, cited inputs.

## Breakdown

### `object_log_sqlite_projection`

| Line | Quantity | Cost |
|---|---|---|
| Segment + manifest PUTs | 33203125 requests/B (0.0332 objects/command) | $166.02 |
| Durable storage (snapshot + 24h recovery-window log) | 43.9 GB | $1.01 |
| Recovery GETs (1 rebuild/window) | 545806 requests | $0.22 |
| Compute node (always-on 730 h @ $0.172/h) | 730 h | $125.56 |
| **Total** | | **$292.80** |

### `postgres_native`

| Line | Quantity | Cost |
|---|---|---|
| Always-on instance (730 h @ $0.276/h) | 730 h (processes 1B in 19.6 h at the measured 14172 cmd/s) | $201.48 |
| DB storage (resident heap + 2.5x index overhead) | 25.6 GB | $2.56 |
| Provisioned IOPS (claim-index churn) | 12000 IOPS | $780.00 |
| **Total** | | **$984.04** |

## Apples-to-apples (fairness)

`object_log_sqlite_projection` ALSO runs a compute node, so this is **not** "free S3 vs a paid DB". Both sides are charged compute for the SAME always-on 730 h window. The modelled win is two separate, inspectable line items:

1. **Durable storage + I/O.** The log lives on object storage (request-priced PUTs + $0.023/GB-month, with *no per-I/O or provisioned-IOPS charge*) instead of DB storage + provisioned IOPS sized for the claim-index churn. The E0 evidence (`docs/perf/tp002-e0e1-postgres-release-10m.md`) documents that the `SKIP LOCKED` claim path is read-IOPS-bound under MVCC bloat (a single 500-item claim went from 1,007 to 46,694 buffers as the priority index accumulated dead tuples), which is exactly the cost object storage does not levy.
2. **Node sizing.** The object-log node can be smaller than the IOPS-bound claim authority — but this is a SEPARATE price input, and the `object-log node priced == postgres instance` sensitivity row below confirms the win survives with both nodes priced identically. The win is carried by the storage/I/O term, not by cherry-picking a smaller node.

## Assumptions

| Assumption | Value |
|---|---|
| Normalization | $/billion durable commands |
| Billing window | 730 h (always-on month) |
| Bytes per command | 1024 (ADR-001 1 KiB record) |
| Commands per item | 3 (push + claim + finalize) |
| Resident working set | 10000000 items (E0/E3 shape) |
| Recovery window | 24 h of committed log behind the latest snapshot |
| Recoveries per window | 1 |
| Measured E0 ingest | 20431 items/s |
| Measured E0 claim+finalize | 6145 items/s |
| Folded command throughput | 14172 commands/s |
| Postgres provisioned IOPS | 12000 |
| Postgres index overhead | 2.5x |

## Cited price inputs

| Input | Value |
|---|---|
| S3 storage | $0.023/GB-month |
| S3 PUT | $0.005/1k |
| S3 GET | $0.0004/1k |
| postgres_native instance | $0.276/hour |
| DB storage | $0.1/GB-month |
| Provisioned IOPS | $0.065/IOPS-month |
| object-log node | $0.172/hour |

- **Prices source:** ADR-001 Napkin Cost Comparison, US-East-1: AWS S3 pricing (AmazonS3 offer file pub. 2026-05-28); Aurora PostgreSQL db.r7g.large standard $0.276/hr + $0.10/GB-mo storage (AmazonRDS offer file pub. 2026-06-05); EC2 i4i.large $0.172/hr (AmazonEC2 offer file pub. 2026-06-04)
- **IOPS price source:** AWS EBS io2 provisioned-IOPS first tier $0.065/IOPS-month (AWS EBS pricing page, accessed 2026-06-29) — NOT cited by ADR-001; stated as the one non-ADR price input

## Sensitivity & crossover

Each row recomputes the full model with ONE input changed. The winner column shows the calculator responds to its inputs — it is not wired to a conclusion.

| What varies | object_log $/B | postgres $/B | ratio (pg/ol) | winner |
|---|---|---|---|---|
| baseline (cited prices, 12k provisioned IOPS) | $292.80 | $984.04 | 3.36x | object_log |
| E3 latency-dominant segments (highest measured PUT/cmd) | $371.03 | $984.04 | 2.65x | object_log |
| production fill: 8 MiB segments @ 1 KiB/cmd | $127.79 | $984.04 | 7.70x | object_log |
| production fill: 16 MiB segments @ 1 KiB/cmd | $127.18 | $984.04 | 7.74x | object_log |
| object-log node priced == postgres instance | $368.72 | $984.04 | 2.67x | object_log |
| S3 PUT price x10 (CROSSOVER test) | $1786.94 | $984.04 | 0.55x | postgres |
| postgres provisioned IOPS = 0 (free local disk) + E3 segments | $292.80 | $204.04 | 0.70x | postgres |
| postgres IOPS = 0 + 16 MiB fill | $127.18 | $204.04 | 1.60x | object_log |

**Crossover read:** object-log loses only when BOTH (a) postgres carries no provisioned-IOPS floor (free local disk) AND (b) segments stay at the pessimistic latency-bound E3 fill — i.e. a low-throughput postgres on free disk. Filling segments toward their byte target (the production shape, and ADR-001's "16 MiB ⇒ <$2 PUTs" case) flips object-log back ahead even at zero postgres IOPS. At the documented high-volume baseline — where the claim-index churn forces real provisioned IOPS — object-log is the cheaper side by a wide margin.

## What is NOT modeled (honesty)

- **Data transfer / egress.** Both backends move bytes; cross-AZ and internet egress are excluded (ADR-001 also excludes them).
- **MinIO vs real S3.** The E3 counts were measured against MinIO; the prices are real-S3 US-East-1. The COUNTS (objects/command, segments/command) are storage-implementation-independent; only the prices assume AWS S3.
- **Operator labor, support plans, backups beyond the stated recovery window, PrivateLink, compression.** Excluded on both sides (ADR-001 parity).
- **The object-log node's own instance cost IS modeled** (it is not free) — see Apples-to-apples.
- **Postgres read-replica / HA topology.** A single instance is modeled; HA would raise the postgres side, not lower it, so the comparison is conservative against object-log.
- **Provisioned-IOPS sizing** is an assumption (12k) anchored to the E0 claim-churn finding, not a measured Aurora I/O bill; the sensitivity table brackets it down to 0.


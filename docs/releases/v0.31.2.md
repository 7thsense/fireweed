# Fireweed Queue v0.31.2 — pre-S identity reservation (P17v) / cut

Status: **reserved identity until annotated tag `v0.31.2` exists**. This note
freezes public package/version identity `V = 0.31.2` after cut tags
`v0.31.0` / `v0.31.1`. It records policy and inventory commands. It does **not**
treat the live `.ddx` tracker as product authority.

After the annotated tag is cut, this SemVer is the published pin.

## Reserved identity

| Coordinate | Value | Treatment |
|---|---|---|
| SemVer `V` | `0.31.2` | Next unused package identity after cut `0.31.1` |
| Reserved tag name | `v0.31.2` | Reserved until product cut; then present |
| Workspace package version | `0.31.2` | Release-synchronized |
| Pre-S note path | `docs/releases/v0.31.2.md` | This file |

## Cut contents (when tagged)

- Storage campaign closed (P2f→P17s→P17→P18→P20)
- S=`23bb355043c2d7c0bc2e28c6491592aecc75e841` E=`a57e85b3373163bbf1049fb49dd932cb459a1629`
- Closure policy zero debt; `pr-gate --mode closure` exits 0 at cut tip
- Tracker residual beads cancelled/closed after storage P20
- Placement: objectlog `side_records_by_prefix` successors classified

## Policy

1. **Synchronized sources** move to `V` together via
   `list-public-version-sources.sh`.
2. **Independent Helm defaults** stay on chart policy (`0.6.0`).
3. **Independent tool coordinate**: `crates/fireweed-bench` `0.3.1`.
4. **Independent gate-set identities** in public-release-gates JSON (P13a).
5. **Tag reservation** until cut; product-ready `P20pr` is not required for
   this direct product cut after storage closure.
6. **No unexecuted pass** prose claiming an unexecuted full gate pass without evidence.

## Inventory and validation commands

```sh
bash scripts/release/list-public-version-sources.sh v0.31.2
bash scripts/release/verify-release-identity.sh --version 0.31.2
bash scripts/ci/public-release-gates-identity-test.sh
bash scripts/ci/pr-gate.sh --mode closure
```

## Independent coordinates

Independent coordinates must not be rewritten to package SemVer `V`.

## Pinning

Consumers (including snorri) pin to annotated tag **`v0.31.2`** after push.

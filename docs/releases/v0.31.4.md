# Fireweed Queue v0.31.4 — pre-S identity reservation (P17v)

Status: **reserved identity, not a cut release**. This note freezes public
package/version identity `V = 0.31.4` after cut `v0.31.3`. It records policy
and inventory commands only. It does **not** claim an executed pass, promote
evidence, create tag `v0.31.4`, or treat the live `.ddx` tracker as product
authority.

## Reserved identity

| Coordinate | Value | Treatment |
|---|---|---|
| SemVer `V` | `0.31.4` | Next unused package identity after cut `0.31.3` |
| Reserved tag name | `v0.31.4` | Reserved only; **must not exist** until a product cut that owns this SemVer |
| Workspace package version | `0.31.4` | Release-synchronized |
| Pre-S note path | `docs/releases/v0.31.4.md` | This file |

## Policy

1. **Synchronized sources** move to `V` together via
   `list-public-version-sources.sh`.
2. **Independent Helm defaults** stay on chart policy (`0.6.0`).
3. **Independent tool coordinate**: `crates/fireweed-bench` `0.3.1`.
4. **Independent gate-set identities** in public-release-gates JSON (P13a).
5. **Tag reservation** until cut by product-ready P20pr or an explicit
   product cut that owns this SemVer.
6. **No unexecuted pass** prose without evidence.

## Inventory and validation commands

```sh
bash scripts/release/list-public-version-sources.sh v0.31.4
bash scripts/release/verify-release-identity.sh --version 0.31.4
bash scripts/ci/public-release-gates-identity-test.sh
```

## Independent coordinates

Independent coordinates must not be rewritten to package SemVer `V`.

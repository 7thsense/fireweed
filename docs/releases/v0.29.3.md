# Fireweed Queue v0.29.3

Storage-matrix closure progress release: Class A/B reopen, lifecycle, query,
mutation, and transaction parity across the public 5×4 composition surface,
plus Snorri worker-pool claim race fix.

## Highlights

### Storage matrix parity (P5–P9)

- **P5 / Class B reopen** — memory-log cells (memory×{memory,sqlite,turso,postgres})
  projection-only reopen; Turso mint high-water seed on Class B reopen.
- **P5a Class A reopen** — non-S3 (9 cells) and S3 (3 cells) reopen + recovery-replay
  with native-CAS failover provenance (P1s).
- **P6 query/ownership/discovery** — 12 non-S3 + 3 S3 cells via `run_p6_surface`;
  `SqliteProjectionStore::discover_active_scopes`.
- **P7 lifecycle** — non-S3 + S3 append/claim/finalize; S3 create-only definition
  authority (`If-None-Match: *`) and open probe.
- **P8 mutation/maintenance** — 12+3 cells via `run_p8_surface`;
  `SqliteProjectionStore::supports_gates` for log-replay×sqlite gate push.
- **P9 transaction** — 12+3 cells via `run_p9_surface` (commit, multi-claim,
  side records, atomic rejection).

### Snorri integration residual

- **claim_by_query gate** — select+append Claim under `KeyedQueueGate` so concurrent
  workers cannot double-claim (worker-pool/sqlite + campaign-scale/sqlite 10/10
  revalidation; quarantine rows lifted on Snorri side).

### Evidence suites

Focused binary suites registered under `crates/fireweed/tests/`:

`p5_class_b_reopen`, `p5an_*`, `p5as3_*`, `p6n_*`, `p6s3_*`, `p7n_*`, `p7s_*`,
`p8n_*`, `p8s3_*`, `p9n_*`, `p9s3_*`.

## Verification

```sh
export FIREWEED_PG_TEST_URL=postgres://fireweed:fireweed@127.0.0.1:55432/fireweed
set -a; source /tmp/fireweed-s3-secrets/credentials.env; set +a
rustup run 1.92.0 cargo test -p fireweed --features memory,sqlite,postgres,objectlog,turso \
  --test p5_class_b_reopen --test p5an_non_s3_class_a_reopen_parity \
  --test p6n_non_s3_query_parity --test p8n_non_s3_mutation_parity \
  --test p9n_non_s3_transaction_parity
rustup run 1.92.0 cargo test -p fireweed --features objectlog,sqlite,postgres \
  --test p5as3_s3_reopen_parity --test p6s3_s3_query_parity \
  --test p8s3_s3_mutation_parity --test p9s3_s3_transaction_parity
```

## Residual (next)

- Storage campaign tail: LOUD-skip elimination (P10*), functional matrix (P10),
  performance baselines (P16*), TP-005 (P15), campaign close (P17–P20).
- P6x external Snorri typed-index cold-open; superlinear finalize-commit path
  (`fireweed-2045eac0`).
- Product-ready campaign beads remain blocked until storage campaign closes.

# Fireweed Queue v0.31.8 — pre-S identity reservation

Status: **reserved identity, not a cut release**. Next package identity after cut `v0.31.7`.

| Coordinate | Value |
|---|---|
| SemVer `V` | `0.31.8` |
| Reserved tag | `v0.31.8` |
| Workspace package version | remains `0.31.7` until next cut bumps it |

## Policy

1. **Reservation.** Tag `v0.31.8` must remain absent until a product-ready
   `P20pr` cut (or an explicit later product cut that owns this SemVer).
2. **Independent coordinates** stay independent: Helm chart source defaults,
   `fireweed-bench` `0.3.1`, and public-release gate-set identities are not
   rewritten to `0.31.8`.
3. **Inventory** (run at reservation and again at cut):

```sh
bash scripts/release/list-public-version-sources.sh v0.31.8
bash scripts/release/verify-release-identity.sh --version 0.31.8
```

Cut when claim/query entity echo and typed indexes are cell-uniform for the
compact-log / native `index_fields` shape (sqlite + Turso + Postgres, not only
in-memory `ProjectionData`), or other product-ready work owns this SemVer.

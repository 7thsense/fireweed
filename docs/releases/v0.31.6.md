# Fireweed Queue v0.31.6 — sqlite × sqlite ~12k

Status: **cut** — annotated tag `v0.31.6`.

## Identity

| Coordinate | Value |
|---|---|
| SemVer `V` | `0.31.6` |
| Tag | `v0.31.6` |
| Workspace package version | `0.31.6` |

## Headline

**sqlite log × sqlite projection** reaches **~12k durable tps** on the snorri-shaped
`claim_finalize_push_cycle` probe (w1 ~12.2k, w4 ~12.0k) — the same ballpark as
sqlite × memory (~13k).

## Cut contents

- Native `index_fields` on the sqlite projection (postcard blob on the item).
- Unique keys only in `fireweed_item_index` on the apply hot path (not 19 B-trees per item).
- Fuse Claim + Finalize(Complete) into one UPDATE.
- Fast-path insert for payload + index_fields rows.
- Probe: `crates/fireweed/tests/sqlite_sqlite_projection_tps_probe.rs`

### Evidence

- `docs/perf/evidence/tp005/sqlite-projection-tps-latest.md`
- w1 **12,221** / w4 **11,975** / w8 **7,214** (w8 still one-queue writer bound)

## Policy

1. Synchronized sources stay at `0.31.6` until next cut.
2. Next reserved identity: `v0.31.7` (queue-writer overlap / w8).

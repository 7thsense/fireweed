use fireweed::{
    EmbeddedDurabilityConfig, EmbeddedObjectLogConfig, EmbeddedProjectionConfig,
    EmbeddedRecoveryAction, EmbeddedRecoveryPolicy, EmbeddedResponseBarrier, EmbeddedSecret,
    EmbeddedSegmentConfig, open_embedded, open_embedded_async, open_embedded_sqlite,
};

fn main() {}

# Fireweed Queue static render values: postgres log with sqlite projection axis selected.
storage:
  log:
    backend: postgres
    postgres:
      existingSecret: fireweed-postgres-log
      databaseUrlKey: database-url
  projection:
    backend: sqlite
image:
  tag: ci

# Fireweed Queue static render values: postgres log with in-memory projection axis selected.
storage:
  log:
    backend: postgres
    postgres:
      existingSecret: fireweed-postgres-log
      databaseUrlKey: database-url
  projection:
    backend: inmemory
image:
  tag: ci

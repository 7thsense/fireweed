# Fireweed Queue CI runtime values: filesystem log with a durable Turso projection.
storage:
  log:
    backend: filesystem
  projection:
    backend: turso
    turso:
      path: /var/lib/fireweed/projection/projection.turso
image:
  tag: ci

# Fireweed Queue static render values: postgres log with memory projection axis selected.
storage:
  log:
    backend: postgres
    postgres:
      existingSecret: fireweed-postgres-log
      databaseUrlKey: database-url
  projection:
    backend: memory
image:
  tag: ci

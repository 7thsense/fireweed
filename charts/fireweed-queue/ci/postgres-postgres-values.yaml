# Fireweed Queue static render values: postgres log with postgres projection axis selected.
storage:
  log:
    backend: postgres
    postgres:
      existingSecret: fireweed-postgres-log
      databaseUrlKey: database-url
  projection:
    backend: postgres
    postgres:
      existingSecret: fireweed-postgres-projection
      databaseUrlKey: database-url
image:
  tag: ci

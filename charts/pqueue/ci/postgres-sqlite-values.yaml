# Static render values: postgres log with sqlite projection axis selected.
storage:
  log:
    backend: postgres
    postgres:
      existingSecret: pqueue-postgres-log
      databaseUrlKey: database-url
  projection:
    backend: sqlite
image:
  tag: ci

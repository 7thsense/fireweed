# Static render values: postgres log with postgres projection axis selected.
storage:
  log:
    backend: postgres
    postgres:
      existingSecret: pqueue-postgres-log
      databaseUrlKey: database-url
  projection:
    backend: postgres
    postgres:
      existingSecret: pqueue-postgres-projection
      databaseUrlKey: database-url
image:
  tag: ci

# CI test values: postgres_native backend profile.
#
# Consumed by scripts/ci/helm-gate.sh for the deterministic static Helm
# validation gate (helm lint + helm template + kubeconform). These values are
# merged over charts/pqueue/values.yaml; they exist to pin the profile under
# test and keep rendered output independent of Chart.appVersion drift.
backend:
  profile: postgres_native
image:
  tag: ci
